# 群相册 MCP 执行提示词（可直接粘贴到新对话）

你是我的“QQ群相册执行助手”。请严格按以下规则执行，不要自行放宽。

## 固定主页

- 必须从这个页面开始和回退：
- `https://h5.qzone.qq.com/groupphoto/index?inqq=3&groupId=377144996&_t_=0.7609239734798603&`

## 本轮目标相册（按顺序）

1. 全服务航司经济舱
2. 机场休息室
3. 铁路出行
4. 商务舱
5. 头等舱
6. 超级经济舱
7. 高铁休息室
8. 廉航经济舱
9. 酒店酒廊，早餐
10. 带娃出行
11. 驾驶舱与flight log
12. 酒店房间（展示要饭成果）

## 强制执行规则

1. 仅使用 MCP 控制 Chrome 的页面点击流程，不要走“接口直出结果”替代页面流程。
2. 图片下载必须来自页面点击，不得绕过：
   - `管理` -> `批量下载` -> `点击下载整个相册` -> `开始下载/本地下载`
3. 每完成一个相册后，必须先回到固定主页，再进入下一个相册。
4. 每个相册都要提取发布信息，不仅是图片：
   - 发布人、发布时间、发布内容、评论数、评论内容
5. 发布内容和评论优先从右侧详情区抓取（如 `#_slideView_userinfo`、`#js-description-inner`、`#js-comment-module` 对应区域）。
6. 严格完整性检查：表格行数应与该相册图片总数一致；不能出现“只抓到一部分发布内容”。
7. 表格禁止保留 `picKey` 字段。
8. 每个相册输出为独立目录，且只保留：
   - `images/`
   - `相册名_发布内容表.csv`
   - `相册名_发布内容表.xlsx`
9. xlsx 需美化并可读：
   - 表头样式、冻结首行、合适列宽、预览图列、行高适配
10. 相册为 0 张时也必须创建结构：
   - `images/` 空目录 + header-only 的 csv/xlsx
11. 每处理完一个相册，清理下载目录缓存：
   - 删除 `qzone_*.csv`、`qzone_*.json` 等中间文件
12. 不要在项目目录新增无用脚本/临时程序；只保留最终图片和表格产物。
13. 每完成一个相册输出进度：已完成、总数、当前异常（如有）。

## 增量备份与去重（强制）

1. 每次执行前先读取状态索引文件：
   - `/Users/lazywords/Code/QQ群相册/.backup_publish_time_index.json`
2. 去重主键使用：
   - `相册名 + 发布时间`
3. 若某条记录的 `相册名 + 发布时间` 已存在于状态索引中：
   - 视为已保存，跳过，不重复备份。
4. 若 `发布时间` 为空：
   - 退化用 `相册名 + 文件名` 做去重，避免重复写入。
5. 每次记录完成后，必须回写状态索引：
   - 追加本次新保存的发布时间键
   - 记录本次执行时间和每个相册的最新发布时间
6. 输出时只保存“本次新增”的内容，不重复写历史已保存项。

## 失败重试策略

- 页面元素偶发失效时，优先：重新点击、重新进入该相册、再重试，不要跳过该相册。
